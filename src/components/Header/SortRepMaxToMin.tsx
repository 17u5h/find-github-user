import React from 'react';
import UiButton from "../ui/UiButton";

const SortRepMaxToMin = () => {

	return (
		<UiButton onClick={() => console.log('maxToMin')}>
			много репозиториев
		</UiButton>
	);
};

export default SortRepMaxToMin;