import React from 'react';
import UiButton from "../ui/UiButton";

const SortRepMinToMax = () => {

	return (
		<UiButton onClick={() => console.log('minToMax')}>
			мало репозиториев
		</UiButton>
	);
};

export default SortRepMinToMax;